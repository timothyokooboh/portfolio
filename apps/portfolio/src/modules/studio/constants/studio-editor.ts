import type { StudioEditorActionState } from "@/modules/writing/types/writing";

export const INITIAL_STUDIO_EDITOR_ACTION_STATE: StudioEditorActionState = {
  fieldErrorKeys: {},
  formErrorKey: null,
};
